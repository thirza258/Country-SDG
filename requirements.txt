# Runtime dependencies for the SDG Country Profiles site.
#
# Everything the site needs to serve its pages is in the first block. The
# integrations block is optional: without the matching API keys those features
# stay switched off and every page renders exactly the same.

Django==5.1.6
asgiref==3.8.1
sqlparse==0.5.3
python-dotenv==1.0.1

pandas==2.2.3
numpy==2.2.3
python-dateutil==2.9.0.post0
pytz==2025.1
six==1.17.0
tzdata==2025.1

gunicorn==23.0.0
whitenoise==6.9.0

# Optional integrations -- AI summary (OPENROUTER_API_KEY) and news (NEWS_API_KEY).
openai==1.65.2

newsapi-python==0.2.7
requests==2.32.3
certifi==2025.1.31
charset-normalizer==3.4.1
idna==3.10
urllib3==2.3.0
pyparsing==3.2.1
